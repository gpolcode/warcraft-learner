{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://warcraft-learner/rulebook.schema.json",
  "title": "warcraft-learner spec rulebook",
  "description": "AI-generated rulebook for a single WoW specialization. Extract all major offensive cooldowns, all personal defensive cooldowns, and 5-10 high-signal rotation/cooldown usage rules from the supplied guide content. Output ONLY the raw JSON object - no markdown fences, no preamble. The first character must be '{' and the last must be '}'.",
  "type": "object",
  "additionalProperties": false,
  "required": ["spec", "spec_icon", "major_cooldowns", "defensives", "rules", "source_summary"],
  "properties": {
    "spec": {
      "type": "string",
      "description": "WCL spec name, e.g. SubtletyRogue. Must match the spec being analysed."
    },
    "spec_icon": {
      "type": "string",
      "description": "Required. The spec's zamimg icon file stem (no extension), e.g. 'ability_stealth'. Read it from the spec's guide page; it becomes the spec's dropdown art. Must be set on every rulebook.",
      "minLength": 1
    },
    "major_cooldowns": {
      "type": "array",
      "description": "REQUIRED and must be populated. All major cooldowns: abilities with a cooldown >= 30s that meaningfully affect damage output (or healing/tanking if applicable). Include on-use trinkets when the guide mentions specific timing for them. Every spec has at least one major cooldown; a spec with an empty list produces no burst/rotation bench, so this must contain at least one item.",
      "minItems": 1,
      "items": { "$ref": "#/$defs/majorCooldown" }
    },
    "defensives": {
      "type": "array",
      "description": "REQUIRED and must be populated. Every personal defensive cooldown with a cooldown >= 15s: immunities, absorb shields, significant damage reductions (>= 20%), and meaningful self-heals. Do NOT include passive talents or stance toggles.",
      "minItems": 1,
      "items": { "$ref": "#/$defs/defensive" }
    },
    "rules": {
      "type": "array",
      "description": "High-signal rotation and cooldown usage rules that are CHECKABLE from cast timing: pairing two abilities, or holding one for an anchor cooldown. Every rule needs a condition, so omit anything that cannot be expressed as one - guidance the engine cannot judge is not a rule. Quality over count: a spec with two real rules is better than one with eight, and an empty list is valid.",
      "items": { "$ref": "#/$defs/rule" }
    },
    "source_summary": {
      "type": "string",
      "description": "2-3 sentences summarising the cooldown strategy these guides recommend."
    },
    "guide_count": {
      "type": "integer",
      "description": "Added automatically by the admin CLI on save - do not populate manually."
    },
    "saved_at": {
      "type": "string",
      "description": "Added automatically by the admin CLI on save - do not populate manually."
    }
  },
  "$defs": {
    "majorCooldown": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "spell_id", "cooldown", "align_with_bloodlust", "usage_rule"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Exact ability name as it appears in-game."
        },
        "spell_id": {
          "type": "integer",
          "description": "WoW spell ID - REQUIRED. Prioritize the Active Ability Cast ID rather than a passive Aura, Classic WoW ID, or Talent Node ID. Verify at wowhead.com/spell=<id>. If genuinely unsure, supply your best guess and record the uncertainty in id_note - NEVER in usage_rule, which is shown to users as coaching copy."
        },
        "cooldown": {
          "type": "number",
          "description": "Cooldown in seconds.",
          "exclusiveMinimum": 0
        },
        "duration": {
          "type": "number",
          "description": "Optional. Active buff/window duration in seconds.",
          "exclusiveMinimum": 0
        },
        "align_with_bloodlust": {
          "type": "boolean",
          "description": "true if the guide explicitly says to sync this with Bloodlust / Heroism / Time Warp. Default false if the guide does not mention it."
        },
        "opener_priority": {
          "type": "integer",
          "description": "Optional. Cast order in the opener (1 = first). Only set if the guide specifies a sequence.",
          "minimum": 1
        },
        "usage_rule": {
          "type": "string",
          "description": "One sentence: when and how to use this cooldown."
        },
        "id_note": {
          "type": "string",
          "description": "Optional. Internal maintainer note for any spell-ID uncertainty (e.g. 'best guess for the Midnight X cast, verify on Wowhead'). NEVER shown to users and not read by any analysis - put verification/best-guess notes here instead of in usage_rule."
        },
        "talent_gated": {
          "type": "boolean",
          "description": "Optional. Set to true when this ability requires a specific talent that not all builds take. When true, having zero casts is treated as normal rather than a 'never used' error."
        }
      }
    },
    "defensive": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "spell_id", "cooldown", "usage_rule"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Exact in-game ability name."
        },
        "spell_id": {
          "type": "integer",
          "description": "WoW spell ID - REQUIRED, same rule as major_cooldowns."
        },
        "cooldown": {
          "type": "number",
          "description": "Cooldown in seconds.",
          "exclusiveMinimum": 0
        },
        "duration": {
          "type": "number",
          "description": "Optional. Buff duration in seconds.",
          "exclusiveMinimum": 0
        },
        "usage_rule": {
          "type": "string",
          "description": "One sentence: when/why to press this button."
        },
        "id_note": {
          "type": "string",
          "description": "Optional. Internal maintainer note for any spell-ID uncertainty (e.g. 'best guess for the Midnight X cast, verify on Wowhead'). NEVER shown to users and not read by any analysis - put verification/best-guess notes here instead of in usage_rule."
        },
        "talent_gated": {
          "type": "boolean",
          "description": "Optional. Set to true when this ability requires a specific talent that not all builds take."
        }
      }
    },
    "rule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "priority", "description", "condition", "action"],
      "properties": {
        "type": {
          "type": "string",
          "description": "Rule category. cooldown_pairing: two abilities must be cast together or in sequence. cd_hold: an ability should be delayed until an anchor cooldown is available. opener: specific sequence or timing required at pull. rotation: ongoing priority or combo maintained throughout the fight. positioning: player must be in a specific location (melee range, behind target, etc.). aoe_switch: behavior changes when multiple targets are present.",
          "enum": ["cooldown_pairing", "cd_hold", "opener", "rotation", "positioning", "aoe_switch"]
        },
        "priority": {
          "type": "string",
          "description": "critical (fundamentally breaks the spec if ignored), high (major DPS loss), medium (moderate optimization), low (minor min-maxing).",
          "enum": ["critical", "high", "medium", "low"]
        },
        "description": {
          "type": "string",
          "description": "Short rule title shown in the UI (<= 60 chars)."
        },
        "condition": {
          "description": "REQUIRED machine-readable trigger. Every rule is judged against the player's cast timeline; a rule that cannot be expressed as one of the kinds below does not belong in the rulebook at all. Do not invent a rule to fill a slot - a short list of checkable rules is correct, an empty list is acceptable.",
          "anyOf": [
            { "$ref": "#/$defs/castWithoutPrior" },
            { "$ref": "#/$defs/holdCooldownForAnchor" }
          ]
        },
        "action": {
          "type": "string",
          "description": "Second-person prescriptive instruction. Must tell the player what to DO, not describe what the rule checks. Good: 'Always cast Secret Technique within 5s of Shadow Dance.' Bad: 'Shadow Dance was cast without Secret Technique beforehand.'"
        }
      }
    },
    "castWithoutPrior": {
      "type": "object",
      "additionalProperties": false,
      "description": "Flag each cast of spell_id that is not accompanied by required_spell_id within window_s seconds, on the side given by position.",
      "required": ["kind", "spell_id", "spell_name", "required_spell_id", "required_spell_name", "window_s"],
      "properties": {
        "kind": { "const": "cast_without_prior" },
        "spell_id": { "type": "integer", "description": "The spell being judged. Every cast of it is either a pass or a violation." },
        "spell_name": { "type": "string" },
        "required_spell_id": { "type": "integer", "description": "The companion spell that must accompany spell_id. Under the default position 'before' it must already have been cast, so the ability that comes FIRST goes here and the ability being judged goes in spell_id." },
        "required_spell_name": { "type": "string" },
        "window_s": { "type": "number", "description": "Seconds within which the required spell must appear, measured from the judged cast.", "exclusiveMinimum": 0 },
        "position": { "type": "string", "enum": ["before", "after", "either"], "default": "before", "description": "Where required_spell_id must sit relative to the judged cast. 'before' (the default) requires it to come first, which is what most rules mean. 'after' requires it to follow. 'either' accepts both sides - use it only when the rule text genuinely says pair or sync rather than naming an order." },
        "exception": {
          "type": "object",
          "additionalProperties": false,
          "description": "Optional. Exempt casts that occur within a specific context window (e.g. during a major 2-minute cooldown window).",
          "required": ["context_spell_id", "context_window_s", "position"],
          "properties": {
            "context_spell_id": { "type": "integer" },
            "context_window_s": { "type": "number", "exclusiveMinimum": 0 },
            "position": { "type": "string", "enum": ["before", "after"] }
          }
        }
      },
      "examples": [
        {
          "kind": "cast_without_prior",
          "spell_id": 280719,
          "spell_name": "Secret Technique",
          "required_spell_id": 185313,
          "required_spell_name": "Shadow Dance",
          "window_s": 8,
          "position": "before",
          "exception": { "context_spell_id": 121471, "context_window_s": 20, "position": "before" }
        }
      ]
    },
    "holdCooldownForAnchor": {
      "type": "object",
      "additionalProperties": false,
      "description": "Flag casts of spell_ids that land within hold_window_s seconds before a non-opener cast of anchor_spell_id.",
      "required": ["kind", "spell_ids", "spell_names", "anchor_spell_id", "anchor_spell_name", "hold_window_s"],
      "properties": {
        "kind": { "const": "hold_cooldown_for_anchor" },
        "spell_ids": { "type": "array", "items": { "type": "integer" }, "description": "Spells that should be held for the anchor." },
        "spell_names": { "type": "array", "items": { "type": "string" } },
        "anchor_spell_id": { "type": "integer", "description": "The anchor cooldown the spells should be saved for." },
        "anchor_spell_name": { "type": "string" },
        "hold_window_s": { "type": "number", "description": "Seconds before the anchor cast within which the held spells are flagged.", "exclusiveMinimum": 0 }
      },
      "examples": [
        {
          "kind": "hold_cooldown_for_anchor",
          "spell_ids": [185313, 280719],
          "spell_names": ["Shadow Dance", "Secret Technique"],
          "anchor_spell_id": 121471,
          "anchor_spell_name": "Shadow Blades",
          "hold_window_s": 15
        }
      ]
    }
  }
}
