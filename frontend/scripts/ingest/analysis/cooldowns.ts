// Moved to the shared burst/bench pipeline (importable by the browser too).
// This re-export keeps existing ingest imports working unchanged.
export * from '../../../src/app/core/analysis/bench/cooldown-casts.ts';
